# RenoP website

Static official site for RenoP (no backend). Built with the same stack as the product UI: **pnpm + Rolldown +
lightningcss**.

## Develop

```powershell
pnpm install
pnpm --filter renop-web build
pnpm --filter renop-web preview
```