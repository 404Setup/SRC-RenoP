{
  "name": "renop",
  "private": true,
  "packageManager": "pnpm@11.20.0",
  "description": "RenoP monorepo root (workspace for web, frontend, shared UI)",
  "scripts": {
    "build:web": "pnpm --filter renop-web build",
    "build:frontend": "pnpm --filter renop-html build",
    "build:ui-consumers": "pnpm --filter renop-web build && pnpm --filter renop-html build"
  }
}
